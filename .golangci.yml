run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - bodyclose
    - noctx
    - exportloopref

linters-settings:
  goimports:
    local-prefixes: github.com/ezhigval

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
